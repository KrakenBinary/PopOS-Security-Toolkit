#!/usr/bin/env bash
register_tool "hydra" "Hydra" "Password Cracking" "hydra" "hydra" \
    "Fast online password brute-forcing tool supporting SSH, FTP, HTTP(S), SMB, LDAP, MS-SQL, MySQL, VNC, SMTP, IMAP, and dozens more protocols with parallelized connections."
